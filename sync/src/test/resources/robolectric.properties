# Native SQLite: the legacy shadow cannot share Room connections across
# worker threads ("Illegal connection pointer"). SDK pinned for stable,
# fast Robolectric runs; the instrumented matrix covers other API levels.
sdk=34
sqlite=NATIVE
