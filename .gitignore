# Gradle
.gradle/
build/
**/build/

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.navigation/

# Machine-local config
local.properties

# Signing — never commit keystores or their passwords
*.jks
*.keystore
key.properties

# Kotlin
.kotlin/

# Logs and OS noise
*.log
.DS_Store
Thumbs.db

# Dev PKI output — generated per-machine, never committed
tools/dev-pki/out/
devserver/ca/out/

# Compiled dev tools
*.exe

# Server runtime material — generated per-deployment, never committed
server/ca/ca.key
server/ca/ca.crt
server/ca/ca.srl
server/ca/server.key
server/ca/server.crt
server/ca/crl.pem
server/ca/.crldb/
server/state/
server/library/
server/.env
*.bak

# Dev-server runtime output — files uploaded during local testing land
# here. They are someone's real camera roll, never repo content.
tools/dev-enroll-server/library/
tools/dev-enroll-server/state/
tools/dev-enroll-server/ca/

# Editor backup files
*~
