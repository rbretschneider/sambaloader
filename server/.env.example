# Copy to .env and adjust. PUBLIC_URL is the only one you must set.

# Public API base URL embedded in enrollment QR codes: what phones dial
# from anywhere. Port 443 implied; the app derives <host>:8443 for
# enrollment on the LAN. Also becomes the server certificate's hostname.
PUBLIC_URL=https://nas.example.com

# Extra names the server certificate should also cover, comma separated.
# Almost always your LAN IP, so enrolling works before DNS resolves
# internally. Changing this reissues the server cert on next start; the
# CA (and every enrolled phone) is unaffected.
EXTRA_SANS=192.168.1.50

# Password for the enrollment/admin page on :8443. Reaching that port is
# NOT permission to add a device — anyone on your wifi can reach it.
# Leave blank and one is generated on first run and printed to the log
# (`docker compose logs uploadd`), which is the safer default: a blank
# value here can never become a weak password.
ADMIN_PASSWORD=

# UID/GID your Samba share expects — find them with `ls -ln` on any file
# that already lives in the share. Wrong values are the #1 self-hosting
# complaint: files land unreadable on the NAS.
PUID=1000
PGID=1000

# Host path of the library (the NAS network mount). NFS strongly
# recommended over SMB — see README "Library mount".
LIBRARY_PATH=./library
